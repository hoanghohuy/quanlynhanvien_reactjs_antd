import React, { Component } from 'react'
import { Button } from 'antd';

export default class Copy extends Component {
  render() {
    return (
        <Button style={{margin: "0 10px 10px 0"}} type="primary" size='large'>Sao chép</Button>
    )
  }
}
